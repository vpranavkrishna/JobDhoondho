<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.delta_inductions.jobdhoondho">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewProfileWorker" android:screenOrientation="portrait"></activity>
        <activity android:name=".NewProfileEmployer" android:screenOrientation="portrait" />
        <activity android:name=".CreateJob" android:screenOrientation="portrait" />
        <activity android:name=".SigninActivity" android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN">
                </action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".UserActivity" android:screenOrientation="portrait"/>
        <activity android:name=".WorkerOrEmployer" android:screenOrientation="portrait"/>
    </application>

</manifest>